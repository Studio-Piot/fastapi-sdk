name: Tests (uv)

on:
  push:
    branches: ["**"]
  pull_request:
    branches: ["**"]

jobs:
  test:
    runs-on: ubuntu-latest

    # 🧩 Start MongoDB for tests
    services:
      mongo:
        image: mongo:7
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.adminCommand(\"ping\")'"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    # Make URI/DB easy to override (repo → Settings → Secrets/Variables)
    env:
      MONGODB_URI: ${{ vars.MONGODB_URI || secrets.MONGODB_URI || 'mongodb://localhost:27017' }}
      MONGODB_DB: ${{ vars.MONGODB_DB  || secrets.MONGODB_DB  || 'test_fastapi_sdk' }}

    strategy:
      matrix:
        python-version: ["3.13"]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # Install the requested Python version (fast on GH runners)
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      # Install uv and enable cache keyed on pyproject/uv.lock, etc.
      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}

      # Install project deps into .venv using lockfile if present
      - name: Sync dependencies (locked if available)
        run: uv sync --frozen

      # Generate test keys if they don't exist (needed for JWT token testing)
      - name: Generate test keys
        run: |
          if [ ! -f test_private_key.pem ] || [ ! -f test_public_key.pem ]; then
            echo "Generating test keys..."
            # Generate encrypted private key (with password, but we'll remove it)
            openssl genrsa -out test_private_encrypted.pem 2048
            # Extract public key
            openssl rsa -pubout -in test_private_encrypted.pem -out test_public_key.pem
            # Extract unencrypted private key
            openssl rsa -in test_private_encrypted.pem -out test_private_key.pem -outform PEM
            # Clean up encrypted key
            rm test_private_encrypted.pem
            echo "Test keys generated successfully"
          else
            echo "Test keys already exist, skipping generation"
          fi

      # Load .env.test if it exists and run tests
      - name: Run tests
        env:
          MONGODB_URI: ${{ env.MONGODB_URI }}
          MONGODB_DB: ${{ env.MONGODB_DB }}
        run: uv run pytest -q

  ci: # <- single gate you will require in branch protection
    needs: [test]
    runs-on: ubuntu-latest
    if: ${{ always() }}
    steps:
      - name: Fail if any prior job failed
        run: |
          if [[ "${{ needs.test.result }}" != "success" ]]; then
            echo "One or more matrix runs failed."
            exit 1
          fi
          echo "All tests passed."
